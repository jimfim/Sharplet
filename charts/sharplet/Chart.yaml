apiVersion: v2
name: sharplet
description: "C# virtual kubelet (Kubernetes node agent) - reference implementation of a virtual node for external systems, with a bundled mock provider that spoofs running pods."
type: application
maintainers:
  - name: jimfim
keywords:
  - virtual-kubelet
  - kubelet
  - node-agent
  - kubernetes
  - csharp
# version and appVersion are rewritten by CI (GitVersion semver via sed, then
# chart-releaser); keep them at 0.0.0 and do not hand-bump.
version: 0.0.0
appVersion: 0.0.0
